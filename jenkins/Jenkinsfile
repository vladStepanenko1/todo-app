pipeline{
    agent{
        docker{
            image 'golang:alpine3.13'
            args '-e HOME=/tmp'
        }
    }

    stages{
        stage('Build'){
            steps{
                sh 'cd cmd'
                sh 'go build -o todo-app'
            }
        }
    }
    post{
        success{
            archiveArtifacts artifacts: 'cmd/todo-app', fingerprint: true
        }
        always{
            deleteDir()
        }
    }
}